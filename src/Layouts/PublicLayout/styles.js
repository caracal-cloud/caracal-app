export let wrapper = {
  px: 6,
  minHeight: '100vh',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  background: `url('${process.env.PUBLIC_URL + '/elephants.jpg'}')`,
  backgroundSize: 'cover'
}

export let content = {
  width: '100%',
  maxWidth: 350
}
